# Backlog: Harbor active-active на KinD

**Статус: в работе — Phase 0–3 выполнены, веха 1 принята пользователем 2026-09-24; Phase 4 (веха 2): H4.1–H4.3 выполнены, дальше H4.4** (создан 2026-09-24). База — `harbor-on-kind` @ `b65df71`: рабочая одно-нодовая лаборатория. Её миграционный backlog (Kind 0.17 → 0.30, K8s 1.26 → 1.34, Harbor 2.8 → 2.15.2; Phase 0–6, все закрыты) сохранён в git-истории этого репозитория и в https://github.com/it255ru/harbor-on-kind.

**Цель:** Harbor в active-active — несколько реплик core / portal / registry / jobservice (и, возможно, trivy) за ingress, с общими внешними PostgreSQL, Redis (Valkey) и S3-совместимым хранилищем. Потеря любой одной реплики не должна прерывать push/pull образов и OCI-чартов.

## Целевая архитектура (требование пользователя)

Источники (2026-09-24): таблица узлов «Harbor (compute)» и архитектурная диаграмма «Harbor / Nexus / Consul / Ceph». Назначение: Docker- и Helm-репозитории для ВМ клиентов в PVE-кластере compute; Harbor **проксирует внешние репозитории** (выход в Интернет — стрелка от «Harbor app») и **хранит локальные** (proxy-cache проекты + обычные проекты).

Что на диаграмме куда ходит:

| Поток | Откуда → куда | Заметка |
|-------|---------------|---------|
| Вход | администраторы и клиенты → **Infra LB** → Harbor (и Nexus) | Infra LB общий для Harbor и Nexus и стоит вне блока Harbor |
| Данные приложения | Harbor app → **Harbor LB** → Postgres Cluster / Redis Cluster | Harbor LB — балансировщик *перед PG и Redis*, а не вход в Harbor |
| Блобы registry | Harbor app → Ceph S3 («Данные») | Ceph internal (№14), S3 API |
| Бэкапы | Postgres Cluster и Redis Cluster → Ceph S3 («Бэкапы») | плюс «Выгрузка бэкапов во внутренний контур» |
| Состояние PG-кластера | Postgres Cluster → Consul (green) («Cluster state») | по стрелке — вероятно, DCS для Patroni (подтвердить) |
| Метрики | Prometheus (green) → Consul (green); «Выгрузка метрик во внутренний контур» | |
| Интернет | Harbor app → Интернет | proxy-cache |

Решением пользователя (D8) строки про бэкапы и метрики в лабораторию не входят — оставлены как часть исходной схемы.

Соответствие таблице узлов: `hb-app-01/02` = Harbor app (жёлтые), `hb-lb-01/02` = Harbor LB (зелёные), `hb-pg-01/02` = Postgres Cluster, `hb-redis-01..03` = Redis Cluster. Цвета на обеих схемах совпадают; легенды нет — смысл (вероятно, зоны/контуры) уточнить (D9).

Вне блока Harbor, но на диаграмме: **Nexus** (proxy, repo, LB, собственный Postgres Cluster), второй Consul/Prometheus («yellow»). Входит ли Nexus в лабораторию — D10 (по умолчанию нет: цель — Harbor).

**Что это меняет в первой версии этого раздела:**
- `hb-lb` — не входная точка Harbor, а балансировщик перед PG/Redis; вход обеспечивает Infra LB. Отсюда `database.external.host` = адрес Harbor LB, проблема «chart принимает один host:port» снимается.
- D4 закрыт по смыслу: блобы в S3 (Ceph RGW). В лаборатории S3-стенд-ин — MinIO (Ceph/Rook слишком тяжёл для одной машины) — подтвердить.
- Появились требования, которых раньше не было: Consul как DCS для Patroni и Infra LB как отдельный уровень (бэкапы и Prometheus есть на схеме, но в лабораторию не входят — D8).

**Ограничение соответствия:** на диаграмме «Redis Cluster», а chart 1.19.2 принимает только `redis` и `redis+sentinel` (режима Redis Cluster в values нет). Фактический режим в проде неизвестен — принят Sentinel как предположение (D3).

## Базовая линия (унаследована, проверена на harbor-on-kind)

| Компонент | Версия |
|-----------|--------|
| Kind CLI | `v0.30.0` |
| Node image | `kindest/node:v1.34.0@sha256:7416a61b42b1662ca6ca89f02028ac133a309a2a30ba309614e8ec94d976dc5a` |
| MetalLB chart | `0.16.1` |
| ingress-nginx chart | `4.15.1` (app `1.15.1`) |
| Harbor chart / app | `1.19.2` / `2.15.2` |

Ключи chart `1.19.2`, относящиеся к HA (сверено по `helm show values harbor/harbor --version 1.19.2`): `database.type: external` + `database.external.*`; `redis.type: external` + `redis.external.*` (встроенный Redis в 2.15.2 — Valkey, `goharbor/valkey-photon`); `persistence.imageChartStorage.type: s3` (для MinIO — `disableredirect: true`, для самоподписанного сертификата хранилища — `caBundleSecretName`); `replicas` у `core`, `portal`, `registry`, `jobservice`, `trivy` (по умолчанию везде `1`).

## Версии компонентов HA (зафиксированы 2026-09-24, H0.1)

Проверено по реестрам (Docker Hub / quay.io / PyPI) и по образам Harbor 2.15.2 на 2026-09-24. Устанавливать только эти версии; менять — сначала здесь и в `CLAUDE.md`.

| Компонент | Pin | Обоснование |
|-----------|-----|-------------|
| PostgreSQL | `postgres:18.6-alpine3.24@sha256:77f585114c32fbca283dc835b0596f4e52b51b4c6662d7810b2f4084f60a1873` | Встроенная БД Harbor 2.15.2 (`goharbor/harbor-db`) — PostgreSQL 18.3; берём тот же мажор 18 |
| Patroni | `4.1.5` (PyPI, extras `consul` + `psycopg3`) | Готового поддерживаемого образа нет (Spilo: последний релиз 2023), поэтому свой образ: слой Patroni поверх образа PostgreSQL выше; версия базового Python — при сборке в Phase 2 |
| Consul | `hashicorp/consul:1.22.7@sha256:a230dcea0bb107bd7958a912d1429fb7f9d399637de7ffb814b34412b9e8c543` | Последний 1.x. Есть 2.0.x (2.0.4), но это свежий мажор: совместимость с Patroni не проверена — не берём |
| HAProxy | `haproxy:3.4.4-alpine3.24@sha256:52c5921e1619f39cbd5b25e1b4b5847667917f39745056cf004d9c263fbf11b9` | Текущий LTS (тег `lts` указывает на 3.4) |
| Valkey (+ Sentinel) | `valkey/valkey:9.0.6-alpine3.24@sha256:187679e3bd4036959631e3f03983ab2ba503ab21e6fd0454d508e909db2ee989` | Встроенный Redis Harbor 2.15.2 — Valkey 9.0.3; тот же мажор. Sentinel — `valkey-sentinel` из того же образа (D3) |
| MinIO | `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e` | Последний обычный релиз с образом на quay.io; на GitHub есть `RELEASE.2025-10-15…`, но образа для него нет |
| MinIO client `mc` | `quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z@sha256:a7fe349ef4bd8521fb8497f55c6042871b2ae640607cf99d9bede5e9bdf11727` | Только для разовой инициализации (бакет, пользователь, политика) из Job; последний релиз с образом на quay.io (тот же источник и та же оговорка о неподдерживаемой ветке, что у MinIO) |

**Заметки:**
- MinIO: community-ветка фактически не развивается (новые образы не публикуются) — годится как учебный стенд-ин для Ceph RGW (D4), но не как эталон. Если понадобится живой проект — вернуться к D4.
- PostgreSQL под Patroni: чарта/оператора не берём, манифесты свои (способ развёртывания — Phase 2). Образы закреплены и по тегу, и по digest.
- Совместимость Harbor 2.15.2 с внешним PostgreSQL 18 подтверждена в H3.3 (миграции прошли, 49 таблиц, push/pull работают).

## Решения и открытые вопросы

**Открытых решений нет** (все закрыты 2026-09-24). Если что-то изменится — вернуть сюда.

**Решено / закрыто:**

- **D6 — критерий успеха в два этапа** (2026-09-24, пользователь): *первым* успехом считается работа всего стенда **с учётом распределения по 14 нодам** (веха 1, ниже); *только после неё* засчитываются остальные проверки (Phase 4, веха 2). Проверки Phase 4 до успеха вехи 1 успехом не считаются.
- **D1 — 14 нод** (2026-09-24): 1 control-plane + 13 воркеров, роль узла — label/taint: `app`×2, `lb`×2, `pg`×2, `redis`×3, `consul`×3, `s3`×1. Consul и MinIO живут на отдельных нодах, Consul — 3 сервера. *Предположение:* MinIO — одна нода (число не называлось). Имена нод в лаборатории — по аналогии с `hb-*` из прод-схемы. Infra LB — ingress-nginx на имеющихся нодах.
- **D2 — Patroni + Consul** (подтверждено пользователем 2026-09-24): PostgreSQL на 2 узла, состояние кластера в Consul. Версии и способ развёртывания — в Phase 2.
- **D3 — Redis: Sentinel (1 master + 2 replica) — ПРЕДПОЛОЖЕНИЕ.** Боевой режим пользователю неизвестен; на схеме «Redis Cluster», а chart 1.19.2 его не поддерживает (только `redis` и `redis+sentinel`). Пересмотреть, если станет известен реальный режим.
- **D4 — блобы registry в S3** (закрыто схемой: Ceph RGW №14; подтверждено: в лаборатории MinIO вместо Ceph).
- **D5 — один лабораторный кластер за раз** (2026-09-24): `CLUSTER=harbor`, `LB_IP=172.20.0.100` и пул `172.20.0.100–110` остаются как в `harbor-on-kind`. Перед `make cluster` здесь — `make cluster-delete` в старом репо (записи в `/etc/hosts` и `insecure-registries` переиспользуются).
- **D7 — Harbor LB = HAProxy ×2** на `lb`-нодах (подтверждено пользователем 2026-09-24). PG: health-check через Patroni REST API (эндпоинт роли primary — сверить по документации выбранной версии Patroni). Redis: проверка роли master. Harbor обращается к PG и Redis по одному адресу Service перед двумя HAProxy; при таком фронте Harbor получает обычный `redis`-адрес (режим `redis+sentinel` остаётся запасным вариантом). **Отклонение от прода:** там общий адрес, вероятно, держит keepalived, в лаборатории — ClusterIP Service. Infra LB — ingress-nginx + MetalLB.
- **D8 — минимальный объём** (2026-09-24): воспроизводим Harbor app ×2, Harbor LB ×2, PostgreSQL ×2 + Consul (Patroni), Redis ×3, S3 для блобов (MinIO), Infra LB. **Бэкапы PG/Redis и Prometheus с диаграммы в лабораторию не входят.**
- **D9 — цвета green/yellow:** для лаборатории не важны; сетевую сегментацию не делаем.
- **D10 — Nexus не входит** (не запрошен; находится вне блока Harbor на диаграмме).

## Критерии успеха (D6)

**Веха 1 — «стенд работает в распределении по 14 нодам».** Принимается, только когда выполнено всё:

1. 14 нод `Ready`; на воркерах роли по label/taint: `app`×2, `lb`×2, `pg`×2, `redis`×3, `consul`×3, `s3`×1.
2. Каждый компонент запущен на ноде *своей* роли (`kubectl get pods -A -o wide`): нет подов Harbor/PG/Redis/Consul/HAProxy/MinIO на чужих нодах; реплики одной роли лежат на *разных* нодах.
3. Кластерные сервисы здоровы: Consul — 3 сервера и есть лидер; Patroni — 1 leader + 1 replica (репликация идёт); Redis — 1 master + 2 replica и кворум Sentinel; HAProxy ×2 оба `Ready`; Harbor core / portal / registry / jobservice — по 2 реплики на двух `app`-нодах; бакет MinIO доступен.
4. Сквозной путь через всю цепочку (Infra LB → Harbor app → Harbor LB → PG/Redis, блобы в MinIO): UI отвечает `200`; push/pull образа и OCI-чарта проходят; `make deploy-app` проходит; блобы лежат в бакете MinIO, а не в томе registry.
5. Нагрузка действительно распределяется: серия pull/запросов через Infra LB обслуживается **обеими** `app`-репликами; HAProxy отдаёт трафик на текущий primary PG.

**Веха 2 — проверки отказоустойчивости** (Phase 4, H4.1–H4.7): выполняются и засчитываются только после успеха вехи 1.

## Последовательный план

Выполнять по фазам. Любой новый компонент (PostgreSQL, Redis, MinIO, операторы) — только с явно зафиксированной pinned-версией здесь и в `CLAUDE.md` до установки.

### Phase 0 — Решения и подготовка

- [x] **H0.1** D6 закрыт, pinned-версии Patroni/PostgreSQL, Consul, HAProxy, Valkey/Sentinel, MinIO зафиксированы (2026-09-24) — раздел «Версии компонентов HA» выше и таблица в `CLAUDE.md`.
- [x] **H0.2** `CLUSTER` / `LB_IP` / MetalLB-пул оставлены как в `harbor-on-kind` (D5). Если HA потребует больше LB-IP — расширить пул в той же подсети и обновить связанные файлы.
- [x] **H0.3** Что шарить между репликами Harbor (2026-09-24; сверено по `helm show values` и шаблонам chart 1.19.2, документации Harbor HA):
  - **Блобы registry:** `persistence.imageChartStorage.type: s3` (+ `disableredirect: true` для MinIO). При `type != filesystem` chart не создаёт PVC registry, а `registry-data` — `emptyDir`; общий том не нужен.
  - **Логи jobservice:** по умолчанию `jobLoggers: [file]` — один RWO-PVC на все реплики, на двух нодах не работает. Ставим `jobLoggers: [database]` (так советует и документация Harbor HA для варианта без RWX); тогда PVC jobservice не создаётся (создаётся только при `file`). RWX StorageClass в KinD не нужен.
  - **Trivy:** StatefulSet со своим `volumeClaimTemplates` (RWO на реплику); общий том не нужен, реплики независимы, очередь сканирования и результаты — в Redis (`SCANNER_STORE_REDIS_URL`). Каждая реплика скачивает свою базу уязвимостей. Решение о числе реплик — H3.2.
  - **Секреты, которые обязаны совпадать у всех реплик и переживать `helm upgrade`:** задавать явно, не полагаться на случайную генерацию: `secretKey` (16 символов, шифрует данные в БД; дефолт `not-a-secure-key` не годится), `core.secret`, `core.xsrfKey`, `jobservice.secret`, `registry.secret`, и **`core.tokenKey` / `core.tokenCert`** (иначе chart на каждом `helm upgrade` генерирует новый token-CA через `genCA`, и во время rolling update реплики core и registry могут подписывать/проверять токены разными ключами). Остальные секреты chart сохраняет через `lookup`, но для воспроизводимого стенда лучше положить всё в `existingSecret`. Пароли лаборатории (`harborAdminPassword`, БД) — как в базовой линии, только для стенда.
  - **Не шарится:** `core` — `emptyDir` (`psc`, только для кэша), `portal` stateless; PVC redis/database chart не создаёт при `external`.
  - **Влияние на план:** H3.1 — `jobLoggers: [database]`, `imageChartStorage.type: s3`, явные секреты выше; H2.5 — MinIO-бакет и ключи доступа до установки Harbor.

### Phase 1 — Кластер

- [x] **H1.0** Предусловия хоста для multi-node KinD (выполнено 2026-09-24): `fs.inotify.max_user_instances=512` и `max_user_watches=524288` (постоянно, в `/etc/sysctl.d/99-kind.conf` — это настройка хоста, в репозитории её нет; на другой машине повторить); свободное место на `/` — 109 ГБ (было 32 ГБ при 97%; каждая нода хранит образы только своей роли — оценка ≈ 5–10 ГБ, не измерялась); RAM: доступно ≈ 31 ГБ из 38 (пользователь подтвердил ~30 ГБ), 16 CPU. Остальные лимиты хоста проверены и достаточны: `fs.file-max`, `kernel.pid_max`, nofile Docker (524288), `TasksMax`, conntrack (262144).
- [x] **H1.0a** Лимиты inotify подняты с запасом под 14 нод (выполнено пользователем 2026-09-24): `fs.inotify.max_user_instances=2048`, `fs.inotify.max_user_watches=1048576` (было 512 / 524288) — постоянно, в `/etc/sysctl.d/99-kind.conf`; значения проверены в живой системе. Это настройка хоста, в репозитории её нет.
- [x] **H1.1** kind-конфиг на 14 нод (D1) — `hack/config/kind-cluster.yaml`, подключён в `make cluster` (2026-09-24). Роль воркера — label `harbor-ha/role=<role>` и taint `harbor-ha/role=<role>:NoSchedule` через `kubeadmConfigPatches` (JoinConfiguration). Имена нод kind: `harbor-worker` (app), `-worker2` (app), `3–4` (lb), `5–6` (pg), `7–9` (redis), `10–12` (consul), `13` (s3). Проверено: создание ≈ 30 с, все 14 нод `Ready`, labels и taints на месте, kindnet/kube-proxy на всех нодах, coredns и local-path-provisioner на control-plane. Замер RAM сразу после создания (`docker stats`): ≈ 2,1 ГБ на 14 контейнеров (control-plane ≈ 576 МБ, воркеры ≈ 115–120 МБ), хост: 29 ГБ доступно; ошибок «too many open files» нет; подсеть `kind` — `172.20.0.0/16`, как в Makefile. Потребление вырастет при установке компонентов — перемерить после Phase 2/3.
  - Из-за taint ingress-nginx и MetalLB speaker сами не запустятся ни на одной воркер-ноде — им нужны tolerations и nodeSelector (H1.2).
- [x] **H1.2** Infra LB на multi-node (2026-09-24): `make infra-lb` (`hack/install-infra.sh`, его же вызывает `hack/install.sh`); values — `hack/config/metallb.yaml`, `hack/config/nginx.yaml`. MetalLB (controller, speaker ×2, frr-k8s) и ingress-nginx ×2 (`podAntiAffinity` по hostname, admission-patch jobs тоже) закреплены на `lb`-нодах через nodeSelector + toleration. Проверено: реплики ingress-nginx на `harbor-worker3` и `harbor-worker4`, Service `172.20.0.100`, IPAddressPool/L2Advertisement на месте. **Решение реализации (не из D1):** Infra LB делит `lb`-ноды с HAProxy (Harbor LB) — отдельных нод под ingress в схеме 14 нод нет; при необходимости вынести — вернуться к D1. Установка пинов та же (MetalLB 0.16.1, ingress-nginx 4.15.1); таймаут ожидания MetalLB поднят с 90 до 180 с (первый старт на 14 нодах медленнее: образы тянутся на каждую ноду).
- [x] **H1.3** LB IP достижим с хоста (2026-09-24): `docker network inspect kind` → `172.20.0.0/16` (совпадает с Makefile и пулом); `curl http://172.20.0.100/` и `https://172.20.0.100/` (-k) → `404` от ingress-nginx (правил Ingress ещё нет — ответ default backend, цепочка MetalLB → ingress работает). ICMP не отвечает (MetalLB L2 не отвечает на ping) — это норма.

### Phase 2 — Общие зависимости

- [x] **H2.1** Consul ×3 (2026-09-24): `make consul` → `hack/ha/00-namespace.yaml` (namespace `harbor-deps` — сюда же пойдут PG/Redis/HAProxy/MinIO) + `hack/ha/consul.yaml` (StatefulSet, образ `hashicorp/consul:1.22.7@sha256:a230…`, свои манифесты без чарта). Реплики на `harbor-worker10/11/12` (все `consul`-ноды, по одной, `podAntiAffinity` + taint toleration), PVC 1 ГБ (`standard`, local-path), PDB `minAvailable: 2`. Клиентский адрес для Patroni — Service `consul.harbor-deps:8500`; headless `consul-headless` для gossip/raft. Проверено: `consul members` — 3 сервера `alive`; `consul operator raft list-peers` — 1 leader + 2 follower (voter), commit index совпадает; из пода в другом namespace (роль `app`) `/v1/status/leader` и `/v1/status/peers` отвечают. Без ACL и TLS (лабораторный стенд; вне D8). Урок: `$(HOSTNAME)` в `args` Kubernetes не подставляет (не входит в env спецификации) — все три сервера получили одно имя и не собрались; имя ноды берётся из `metadata.name` через `POD_NAME`.
- [x] **H2.2** PostgreSQL ×2 под Patroni + Consul (2026-09-24): `make postgres` = `make pg-image` (сборка `hack/ha/patroni/Dockerfile`: `postgres:18.6-alpine3.24@sha256:77f5…` + Patroni 4.1.5 в venv, зависимости зафиксированы в `constraints.txt`; образ `harbor-ha/patroni:4.1.5-pg18.6` загружается `kind load` только в `pg`-ноды, `imagePullPolicy: Never`) + `hack/ha/postgres.yaml` (StatefulSet ×2, `podAntiAffinity`, PVC 5 ГБ, PDB `minAvailable: 1`, headless Service `pg-headless`, конфиг Patroni в ConfigMap; DCS — `consul.harbor-deps:8500`, scope `harbor-pg`). Пароли (`superuser`, `replication`, `harbor`) — Secret `pg-credentials`, генерируется `openssl rand` при первом `make postgres`, в git не попадает. `post_bootstrap` создаёт роль `harbor` и БД `registry` (владелец `harbor`). Проверено: поды на `harbor-worker6` (pg-0) и `harbor-worker5` (pg-1); `patronictl list` — pg-0 Leader/running, pg-1 Replica/streaming, lag 0; REST: `/primary` → 200 на лидере и 503 на реплике, `/replica` → 200 на реплике (это health-check для HAProxy, H2.4); ключи `service/harbor-pg/*` в Consul; под ролью `harbor` на primary работают `CREATE EXTENSION "uuid-ossp"` и запись, строка видна на реплике (`pg_is_in_recovery = t`). Репликация асинхронная (`synchronous_mode` не включён — вне D8). **Ограничения:** образ живёт в containerd нод и пропадает при пересоздании кластера (`make postgres` пересобирает и загружает заново); `pg_hba` пускает по паролю (scram) только из pod-сети `10.244.0.0/16`. Совместимость Harbor 2.15.2 с внешней PostgreSQL 18 — проверить в H3.3 (миграции).
- [x] **H2.3** Valkey ×3 + Sentinel (D3, предположение) (2026-09-24): `make redis` → `hack/ha/redis.yaml` (StatefulSet ×3 на `redis`-нодах, в каждом поде два контейнера: `valkey` и `sentinel` — sidecar, решение пользователя; образ `valkey/valkey:9.0.6-alpine3.24@sha256:1876…`, `podAntiAffinity`, PVC 1 ГБ, `appendonly yes`, PDB `minAvailable: 2`, headless Service `redis-headless`). Пароль — Secret `redis-credentials` (`openssl rand` при первом запуске, в git не попадает); `requirepass` + `masterauth` + `auth-pass` в Sentinel. Сами Sentinel без пароля (доступны только из pod-сети). Топология через стабильные DNS-имена подов (`resolve-hostnames`/`announce-hostnames`, `replica-announce-ip`), а не через pod IP; старт-скрипты в ConfigMap `redis-scripts`: при старте спрашивают Sentinel о текущем master, иначе `redis-0` — master, остальные — реплики `redis-0`; `sentinel.conf` хранится на PVC. Проверено: поды на `harbor-worker7/8/9`; `redis-0` role:master с 2 подключёнными репликами, `redis-1/2` role:slave, `master_link_status:up`; `sentinel master mymaster` — flags master, num-slaves 2, num-other-sentinels 2, quorum 2; `sentinel ckquorum` — «3 usable Sentinels»; без пароля `NOAUTH`; запись на master читается с реплики. **Не проверено (Phase 4 / H4.7):** фактический failover при потере master и возврат старого master как реплики; в H2.4 HAProxy для Redis определяет master по `ROLE`/`INFO replication` с `AUTH`.
- [x] **H2.4** Harbor LB — HAProxy ×2 (D7) (2026-09-24): `make harbor-lb` → `hack/ha/haproxy.yaml` (Deployment ×2 на `lb`-нодах, `podAntiAffinity`, PDB `minAvailable: 1`; образ `haproxy:3.4.4-alpine3.24@sha256:52c5…`; конфиг в ConfigMap `haproxy-config`, DNS бэкендов — через `resolvers` из `resolv.conf`, `init-addr none`). Единый адрес для Harbor: Service `harbor-lb.harbor-deps` — `:5432` (PG) и `:6379` (Redis). PG: HTTP-check `GET /primary` на Patroni REST `:8008` (200 только у лидера), `on-marked-down shutdown-sessions`. Redis: tcp-check `AUTH` (пароль из Secret `redis-credentials` через env) → `PING` → `INFO replication` → ждёт `role:master`. Проверено: поды на `harbor-worker3` и `harbor-worker4` (обе `lb`-ноды); в `/stats` у обоих HAProxy `postgres`: pg-0 UP / pg-1 DOWN, `redis`: redis-0 UP / redis-1,2 DOWN; через `harbor-lb.harbor-deps:5432` запрос попадает на primary (`pg_is_in_recovery = f`, IP pg-0) даже при вызове из пода-реплики; через `:6379` — `set`/`role:master`/`del`; из namespace `default` на `app`-ноде `PING` → `PONG`. Web-статистика — `harbor-lb:8404/stats`. Не проверено (Phase 4 / H4.7): переключение при отказе primary PG / master Redis / одного HAProxy. Замечание: Service — обычный ClusterIP (в проде общий адрес, вероятно, keepalived — отклонение из D7).
- [x] **H2.5** MinIO на `s3`-ноде + бакет (2026-09-24): `make minio` → `hack/ha/minio.yaml` (StatefulSet ×1 на `harbor-worker13`, образ `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z@sha256:14ce…`, PVC 10 ГБ, Service `minio.harbor-deps:9000`, консоль `:9001`; Job `minio-init` на образе `mc` `RELEASE.2025-08-13T08-35-41Z@sha256:a7fe…` — новый компонент, версия записана в таблицу выше до установки). Job создаёт бакет **`registry-blobs`**, политику `registry-blobs-rw` (только этот бакет) и пользователя `harbor` с ней; идемпотентен (повторный `make minio` проходит, Job пересоздаётся). Учётные данные — Secret `minio-credentials` (`root-user`/`root-password`, `harbor-access-key`/`harbor-secret-key`; пароли `openssl rand`, в git не попадают). Проверено: под пользователем `harbor` из пода на `app`-ноде `mc cp`/`cat`/`rm` в `registry-blobs` проходят, создание другого бакета — `Access Denied`. **Для H3.1:** `persistence.imageChartStorage.s3`: `regionendpoint: http://minio.harbor-deps:9000`, `bucket: registry-blobs`, `region: us-east-1`, `secure: false`, `disableredirect: true`; chart ждёт `existingSecret` с ключами `REGISTRY_STORAGE_S3_ACCESSKEY` / `REGISTRY_STORAGE_S3_SECRETKEY` — имена ключей в `minio-credentials` другие, поэтому в H3.1 создать отдельный Secret для Harbor из `harbor-access-key`/`harbor-secret-key` (Secret должен лежать в namespace Harbor, не `harbor-deps`). Одна нода, один диск, без erasure coding — отказоустойчивости у хранилища нет (осознанно: D1/D8).
- [x] **H2.6** Доступность из namespace Harbor (2026-09-24; Harbor ставится в `default`, как в baseline): тестовые поды в `default` на `app`-ноде (с toleration роли `app`), образы — те же pinned. PostgreSQL: вход под `harbor` в БД `registry` через `harbor-lb.harbor-deps:5432` → primary (`not pg_is_in_recovery = true`); Consul: `consul.harbor-deps:8500/v1/status/leader` → лидер; Redis: `set`/`get`/`del` с паролем через `harbor-lb.harbor-deps:6379`; MinIO: `mc ls` бакета `registry-blobs` под пользователем `harbor` через `minio.harbor-deps:9000` → ok. Адреса для values Harbor: БД `harbor-lb.harbor-deps:5432` (БД `registry`, пользователь `harbor`), Redis `harbor-lb.harbor-deps:6379` (пароль из `redis-credentials`), S3 `http://minio.harbor-deps:9000`. Секреты живут в `harbor-deps`, а Harbor читает их из своего namespace — в H3.1 скопировать/создать нужные Secret в `default` (пароль БД `harbor`, пароль Redis, ключи S3), не хардкодить в values. **Phase 2 закрыта.**

### Аудит перед Phase 3 (2026-09-24)

- Полная пересборка с нуля (`cluster-delete` → `cluster` → `infra-lb` → `ha-deps`) прошла без ручных шагов: ≈ 7 мин (`cluster` + `infra-lb`) и ≈ 4 мин (`ha-deps`), время в основном — загрузка образов. Добавлен таргет `make ha-deps` (порядок: consul → postgres → redis → harbor-lb → minio).
- После пересборки: 14/14 `Ready`; нет подов не в `Running`/`Completed` и нет рестартов; каждый под на ноде своей роли, реплики на разных нодах; Consul — 1 leader + 2 follower; Patroni — Leader + Replica (streaming, lag 0); Sentinel — «3 usable Sentinels»; оба HAProxy — PG и Redis по одному UP (primary/master); `minio-init` — `init done`; `http://172.20.0.100/` → 404 (ingress жив), подсеть `kind` `172.20.0.0/16`.
- Все манифесты `hack/ha/*.yaml` и `lb-ipaddresspool.yaml` проходят `kubectl apply --dry-run=server`; `yamllint` чист; `bash -n` для скриптов ок; секретов и паролей в репозитории нет (пароли генерируются в Secret).
- Замер памяти (`docker stats`) с Phase 0–2 без Harbor: ≈ 3,9 ГиБ на 14 нод (control-plane ≈ 740 МиБ, `lb`-ноды ≈ 500–540 МиБ, `pg` ≈ 290–310, `s3` ≈ 360, остальные ≈ 120–180); на хосте свободно ≈ 27 ГБ, диск — 97 ГБ. Оценка «≈ 12–13 ГиБ» относилась ко всему стенду с Harbor — перемерить после H3.3.
- Найдено и исправлено при подготовке ранбука: под `metallb-frr-k8s-statuscleaner` не имел закрепления за `lb` (subchart frr-k8s игнорирует nodeSelector для него) — в `hack/config/metallb.yaml` добавлен `nodeAffinity` (`frr-k8s.frrk8s.affinity`), применено `make infra-lb` (helm revision 2), под перезапущен на `lb`-ноде, ingress и MetalLB работают (`http://172.20.0.100/` → 404).
- Замеченное и не ошибка: при холодном старте образов `ErrImagePull`/`ImagePullBackOff` (сбой токена Docker Hub, разовый `NotFound` на quay.io) — Kubernetes ретраит, поды поднимаются сами.
- Известные ограничения перед Phase 3: baseline-`make install` / `make deploy-app` на этом кластере не работают (нет tolerations в `harbor.yaml`) — чинится в H3.1; правка конфига HAProxy требует ручного bump аннотации `config-version`; failover PG/Redis/HAProxy не проверялся (веха 2).

### Phase 3 — Harbor в HA

- [x] **H3.1** `hack/config/harbor-ha.yaml` + `hack/install-harbor-ha.sh` (`make harbor-ha`, `DRY_RUN=1` — только рендер по кластеру) (2026-09-24). Ключи chart 1.19.2 сверены по `helm show values`/шаблонам. Содержимое: внешняя БД `harbor-lb.harbor-deps…:5432` (БД `registry`, пользователь `harbor`), внешний Redis `harbor-lb.harbor-deps…:6379` (режим `redis`, не sentinel), `imageChartStorage.type: s3` (MinIO, `disableredirect: true`, `regionendpoint http://minio.harbor-deps…:9000`, `secure: false`, бакет `registry-blobs`), `jobservice.jobLoggers: [database]`, `replicas: 2` у core/portal/registry/jobservice + `nodeSelector app` + toleration + `topologySpreadConstraints` по hostname (`maxSkew: 1`, `matchLabelKeys: [pod-template-hash]`; изначально был `podAntiAffinity`, заменён в H3.3 из-за блокировки rolling update, а `matchLabelKeys` добавлен в H3.5, иначе после rollout обе реплики могли оказаться на одной ноде) + PDB `minAvailable: 1`; Trivy — `replicas: 1` на `app`-ноде (решение H3.2); ingress `className: nginx`, TLS `auto`. **Секреты** (скрипт создаёт один раз в `default`, никогда не пересоздаёт): `harbor-ha-secrets` (пароль БД, пароль Redis, `secret`, `CSRF_KEY`, `JOBSERVICE_SECRET`, `REGISTRY_HTTP_SECRET`, `secretKey` — случайные, фиксированные между `helm upgrade`), `harbor-ha-s3` (только S3-ключи: chart подмешивает этот Secret в registry целиком через `envFrom`, поэтому отдельный), `harbor-ha-token` (TLS-пара для подписи токенов, общая для всех реплик core; иначе chart на каждом upgrade генерирует новую). **Найдено при проверке:** (1) chart читает `existingSecret` для Redis/БД через `lookup` при рендере — Secret должен существовать до `helm install`, а `helm template` без кластера даёт пустой пароль в `_REDIS_URL_*`; проверка — `helm upgrade -i --dry-run=server` (пароль подставляется во всех трёх местах: core, registry, trivy). (2) `database.maxOpenConns` по умолчанию 900 на pod при `max_connections=200` у PG — выставлены `maxIdleConns: 20`, `maxOpenConns: 50` (2 core ≈ 100 < 200). (3) chart-овский `nginx` при `expose.type: ingress` не разворачивается. **Проверено (без установки):** рендер даёт 4 Deployment (core/jobservice/portal/registry по 2 реплики, все на `app`, с anti-affinity) + StatefulSet trivy ×1 на `app`, нет внутренних `harbor-database`/`harbor-redis`, dry-run на сервере проходит (28 объектов), Harbor-релиза в кластере нет. Реальная установка и `Ready` — H3.3.
- [x] **H3.2** Trivy: одна реплика (решение пользователя, 2026-09-24). Обоснование: StatefulSet со своим RWO-томом на реплику, общий том не нужен, реплики независимы (очередь и результаты — в Redis), но каждая реплика отдельно качает базу уязвимостей; Trivy не входит в критерии вехи 1 (D6). Число реплик — `trivy.replicas` в `hack/config/harbor-ha.yaml`; вернуться, если понадобится проверять отказ сканера (веха 2).
- [x] **H3.3** Установка Harbor 1.19.2 (app 2.15.2) в HA (2026-09-24): `make harbor-ha` (и `make install` = `install-infra.sh` + `install-harbor-ha.sh`). Итог: core/portal/registry/jobservice по 2 реплики `Running`, по одной на `harbor-worker` и `harbor-worker2` (обе `app`), trivy ×1 на `app`, внутренних `harbor-database`/`harbor-redis` нет; UI `https://core.harbor.domain/` → 200. Harbor работает на внешних сервисах: миграции прошли на PostgreSQL 18 (49 таблиц в `registry`, проект `library`, 2 пользователя; реплика PG их видит, `pg_is_in_recovery = t`), в Redis на master заняты db0 (68 ключей) и db1 (28). **Найдено и исправлено при установке:** (1) **ключ токена в формате PKCS#8** — `openssl req -newkey` даёт `BEGIN PRIVATE KEY`, core отвечает 500 на `/v2/` (`unable to get PrivateKey from PEM type: PRIVATE KEY`); теперь `openssl genrsa -traditional` (PKCS#1, `BEGIN RSA PRIVATE KEY`), Secret `harbor-ha-token` пересоздан. Если у кого-то Secret создан старой версией скрипта — удалить его и запустить `make harbor-ha`. (2) **Взаимная блокировка rolling update при обязательном `podAntiAffinity` на 2 нодах**: surge-поду некуда встать (нужна 3-я нода), а `maxUnavailable` от 25% при 2 репликах округляется до 0; ещё и старые поды несут прежнее правило (сообщение «didn't satisfy existing pods anti-affinity rules»). Исправление: для core/portal/registry/jobservice — `topologySpreadConstraints` (`maxSkew: 1`, `DoNotSchedule`) вместо anti-affinity; для `harbor-lb` (2 `lb`-ноды, anti-affinity сохранён) — `strategy: RollingUpdate, maxSurge: 0, maxUnavailable: 1`. При переходе пришлось один раз пересоздать Deployment'ы (`scale 0` → `2`); проверено, что после этого `rollout restart` core и registry и bump `config-version` у `harbor-lb` проходят. (3) jobservice при первом старте делает 2–3 рестарта (core ещё не принимает соединения: `connection refused` на `harbor-core:80/api/v2.0/internalconfig`) и затем работает — штатная гонка запуска, не ошибка.
- [x] **H3.4** Доверие CA, `make deploy-app` (2026-09-24): хост уже доверял `core.harbor.domain` (`insecure-registries`, `/etc/hosts`), `make deploy-app` проходит за ≈ 15 с: проект `python`, `docker login`/build/push `core.harbor.domain/python/hello:1.0` (digest `sha256:4a841ff3…`), CA Harbor в containerd control-plane-ноды, pull secret, деплой. **Решение реализации (новое, не из D1):** demo-приложение запускается на **control-plane** — воркеры под таинтами ролей, а `deploy-app.sh` ставит CA только на control-plane; поэтому в `python-docker-hello-kube/deployment.yml` и `helm-hello-kube` (values `nodeSelector`/`tolerations`, шаблоны deployment и test-pod) добавлены selector и toleration `node-role.kubernetes.io/control-plane`. Установка CA на все ноды не делалась. Проверено: `hello-service` `172.20.0.101:5000` отвечает от разных подов; OCI-чарт: `helm registry login` → `helm package` → `helm push oci://core.harbor.domain/python/hello` → `helm pull` (тот же digest `sha256:3cb1f37d…`) → `helm install` из OCI → `helm test` (Phase: Succeeded) → `helm uninstall`. **Блобы в MinIO, а не в томе:** `mc ls -r registry-blobs` — 16 объектов, 17 МиБ (среди них блоб с digest манифеста образа `4a841ff3…`); PVC registry нет (единственный PVC Harbor — `data-harbor-trivy-0`).
- [x] **H3.5** Приёмка вехи 1 (2026-09-24): все пять пунктов «Критериев успеха (D6)» выполнены; результат ниже. **Принята пользователем 2026-09-24** — с этого момента проверки Phase 4 (веха 2) засчитываются.
  1. **14 нод, роли.** 14/14 `Ready`; воркеры: `app` 2, `lb` 2, `pg` 2, `redis` 3, `consul` 3, `s3` 1 (+ control-plane без роли), на всех 13 воркерах таинт роли.
  2. **Размещение.** Проверка V2.4 из `docs/verification-runbook.md` — пусто (нет подов не на своей роли; ingress-nginx и MetalLB — на `lb`); повторов «одна роль — одна нода» нет: consul-0/1/2 на трёх `consul`-нодах, pg-0/1 на двух `pg`, redis-0/1/2 на трёх `redis`, harbor-lb ×2 и ingress-nginx ×2 на двух `lb`, minio-0 на `s3`; core/portal/registry/jobservice — по одной реплике на `harbor-worker` и `harbor-worker2`. **Найдено и исправлено при приёмке:** после `rollout restart` обе реплики core оказались на одной ноде — `topologySpreadConstraints` не перераспределяет запущенные поды, а старая и новая ревизии считались вместе. Исправление: `matchLabelKeys: [pod-template-hash]` во всех четырёх компонентах Harbor (revision 4 релиза); проверено — 5 подряд `rollout restart` всех четырёх Deployment'ов, после каждого ровно 1+1 на двух `app`-нодах.
  3. **Кластерные сервисы.** Consul: consul-0 leader, consul-1/2 follower; Patroni: pg-0 Leader/running, pg-1 Replica/streaming; Redis: master + 2 slave, Sentinel `ckquorum` — «3 usable Sentinels»; HAProxy ×2 `1/1 Running`, у обоих postgres: pg-0 UP / pg-1 DOWN, redis: redis-0 UP / redis-1,2 DOWN; Harbor: core, portal, registry, jobservice — по `2/2`, trivy `1/1`; MinIO `minio-0` Running, `minio-init` Complete, бакет `registry-blobs` доступен; неисправных подов 0.
  4. **Сквозной путь через Infra LB.** UI `https://core.harbor.domain/` → 200, `/api/v2.0/systeminfo` → 200, `/v2/` → 401 (нужен токен); `docker login` успешен; `make deploy-app` проходит (push `hello:1.0`, digest `sha256:4a841ff3…`); `docker rmi` + `docker pull` возвращают тот же digest; OCI-чарт: `helm push` → `helm pull` (digest `sha256:d6bcfdc7…`), ранее также `helm install` из OCI и `helm test` → Succeeded; блобы в MinIO: `mc du registry-blobs` — 26 объектов, 17 МиБ; PVC registry нет (единственный PVC Harbor — `data-harbor-trivy-0`).
  5. **Распределение нагрузки.** 100 запросов к `/api/v2.0/systeminfo`, 100 к `/`, 60 к `/v2/` и 20 `docker pull` через Infra LB; по адресу получателя в логах ingress-nginx: core — 127 и 131 запросов на двух подах, portal — 50 и 50; по логам registry `GET /v2/`: 20 и 18 (обе реплики); оба ingress-nginx обработали трафик (239 и 175 записей). HAProxy держит на primary: у пользователя `harbor` на `pg-0` 9 соединений (от 2 адресов — обоих HAProxy), на `pg-1` — 0.
  Ограничение приёмки: failover, потеря реплики/ноды и остальные проверки отказоустойчивости не выполнялись (это веха 2, Phase 4).

### Phase 4 — Проверка отказоустойчивости (веха 2: начинать только после H3.5)

- [x] **H4.1** push/pull образа и OCI-чарта при всех репликах (2026-09-24, веха 2; засчитывается — веха 1 принята). Условия: все реплики работают (core, portal, registry, jobservice — `2/2`, trivy `1/1`, зависимости `Running`). Ход: собраны два образа `hello:h41-a` / `h41-b` с разными слоями по 40 МБ случайных данных (чтобы registry делал multipart-загрузку в S3, chunk по умолчанию 5 МБ), запушены **параллельно** — оба успешно (digest `sha256:9bc1d710…` и `sha256:f90e67ca…`); после `docker rmi` оба скачаны обратно с теми же digest; в API Harbor артефакты есть (размер ≈ 59,7 МБ в сжатом виде); под из `h41-a` (pull делает containerd ноды через pull secret) стал `Ready`, `imageID` совпал с digest; OCI-чарт `hello-kube` 0.1.1: `helm push` → `helm pull` (один и тот же digest `sha256:99d111f3…`) → `helm install` из OCI → `helm test` (Phase: Succeeded) → `helm uninstall`. **Обе реплики участвовали:** registry — PATCH 2 и 2, PUT 5 и 4, GET 12 и 9; core (`/v2/` через ingress, по адресу получателя) — 35 и 31. Блобы в MinIO: 17 МиБ → 97 МиБ (44 объекта). Тестовые артефакты (`h41-a`, `h41-b`, чарт 0.1.1) удалены через API, тестовые поды убраны. Замечание: удаление артефакта в Harbor не освобождает блобы в S3 — место вернёт только сборка мусора (GC) registry; в лаборатории её не запускали. В API репозитория чарта остаётся один безымянный артефакт (перезаписанный при повторном пуше 0.1.0 в H3.5) — не связан с H4.1.
- [x] **H4.2** Удаление пода registry/core *во время* push (2026-09-24, веха 2). Скрипт: `hack/tests/h42-kill-during-push.sh <registry|core> <container> <tag>` (строит образ с несжимаемыми слоями, ограничивает скорость app-нод через `tc`, чтобы загрузка длилась ~2 минуты, по росту `eth0 rx` находит под, который *сейчас принимает* загрузку, и убивает его `--force --grace-period=0`; после push сравнивает digest, чистит за собой по digest). Образ: 2 слоя по 200 МБ.
  - **registry:** убит `harbor-registry-…-sw8rn` (`harbor-worker2`) в 11:06:17 UTC, через ~0,005 с после обнаружения активной загрузки (rx +27 МБ за опрос). `docker push` продолжил и завершился в 11:08:19 с `exit=0` и digest `sha256:85637748…`; клиент сам повторил загрузку слоя (15 строк `Retrying in N s`). ingress-nginx: 2 × `PATCH 502` в момент убийства, затем `PATCH 202` ×3, `PUT 201` ×4. После `docker rmi` образ скачан обратно — digest тот же, оба слоя по 209 715 200 байт. Реплика пересоздана (`…-5tf6r`, `harbor-worker2`), Deployment `2/2`.
  - **core:** убит `harbor-core-…-lc8lj` в 11:15:14 UTC (rx +4 МБ за опрос). `docker push` завершился в 11:17:35, `exit=0`, digest `sha256:a7bca236…`, 5 строк `Retrying`; ingress: `PATCH 502` ×1 (плюс запись `upstream …` о закрытом соединении), затем `PATCH 202` ×3, `PUT 201` ×4. Pull обратно — digest совпал, слои 209 715 200 байт. Реплика пересоздана (`…-kjlp9`, `harbor-worker`).
  - **Вывод:** потеря активной реплики registry или core посреди загрузки не приводит к потере или порче данных: клиент получает 502, повторяет запрос, загрузка завершается на оставшейся реплике. Push «корректно повторяется» (критерий пункта выполнен). Push в Harbor при этом не был прозрачным: клиент видел ошибку и делал повтор (~15 с для слоя), так что «без ошибок у клиента» для H4.2 не заявляется.
  - **Не засчитанные попытки (для честности):** три прогона без убийства (push успевал завершиться до обнаружения; на localhost 1–4 ГБ уходят за секунды; в контейнере registry нет `awk`, детектор сначала читал пустое значение), один прерванный оборвавшейся сессией (3 слоя в ретраях, итог неизвестен), одна попытка на core, испорченная сбоем Redis от перегрузки диска (см. ниже: push отклонён с `unauthorized`, убийства не было); повторена после стабилизации и прошла.
  - **Побочные находки (важные для всей веха 2):** (1) **Перегрузка общего диска хоста дестабилизирует весь стенд.** Гигабайты записи при тестах (сборка образов с `dd`, push по 1–4 ГБ) поднимали load average до 30+. Следствия: `kube-controller-manager` и `kube-scheduler` теряли leader-election (`leaderelection lost`, 7 рестартов, `CrashLoopBackOff`) — Kubernetes на минуты переставал пересоздавать поды (реплика после убийства не воссоздавалась, пока control-plane не вернулся); Valkey писал `Asynchronous AOF fsync is taking too long (disk is busy?)`, Sentinel несколько раз переключал master (redis-0 → redis-1 → redis-2), HAProxy каждый раз сам находил нового master; у core, jobservice (7–9), pg, frr-k8s и apiserver были рестарты по liveness-проб. Меры: **leader-election тайминги** `lease 60s / renew 40s / retry 10s` для controller-manager и scheduler (`hack/config/kind-cluster.yaml`, кластерный `kubeadmConfigPatches`; на живом кластере применено правкой static-pod манифестов — после этого рестартов нет); **`down-after-milliseconds` Sentinel 5000 → 15000** (`hack/ha/redis.yaml` и `SENTINEL SET` на всех трёх Sentinel в живом кластере). Оба изменения — настройка под лабораторию с одним диском, а не архитектурное решение; влияют на H4.7 (Redis-master теперь обнаруживается за ~15 с вместо ~5 с). Изменение в `kind-cluster.yaml` **не проверено пересборкой кластера** — проверится в H5.3. (2) **Ошибка очистки:** тег `h42-auth` был поставлен на образ `hello:1.0` (один digest), а удаление артефакта в Harbor удаляет его целиком со всеми тегами — пропал `hello:1.0`; восстановлен `make deploy-app` (тот же digest `sha256:4a841ff3…`). Скрипт теперь удаляет только по digest собственного образа. (3) В MinIO остались 14 нулевых `_uploads/<uuid>/data` (незавершённые multipart); есть и у пушей без убийства (10:43, 11:02 UTC), то есть это штатный мусор S3-драйвера registry, а не следствие убийств; убирается purge-задачей registry/GC. Бакет вырос до 8,5 ГиБ (121 объект) из-за не запускавшегося GC после удаления тестовых артефактов. (4) HAProxy/Patroni/Consul при этом не пострадали.
- [x] **H4.3** Rolling update core и registry во время непрерывных pull (2026-09-24, веха 2). Скрипт: `hack/tests/h43-rolling-update.sh` (+ `h43_analyze.py`): через Infra LB идут три нагрузки — `GET /v2/python/hello/manifests/1.0` каждые ~0,1 с, `GET` самого большого слоя (13 МБ, registry отдаёт из MinIO) каждые ~0,4 с и `docker rmi` + `docker pull python/hello:1.0` подряд; curl **не** повторяет запросы, поэтому каждая ошибка — это ошибка, которую увидел бы клиент; `kubectl rollout restart` core, затем registry, нагрузка идёт до, во время и после. Нагрузка лёгкая (диск общий, см. H4.2).
  - **Первый замер (без исправления), 4 прогона:** core — ошибок нет ни в одном прогоне; registry — в 2 прогонах из 4 по одной ошибке: `502` на запрос манифеста и `docker pull` с `received unexpected HTTP status: 502 Bad Gateway` (всего 1/749 запросов манифеста, 0/340 блоба, 1/350 pull), плюс паузы запросов ≈ 5,2–5,3 с. Причина: при остановке пода registry kube-proxy ещё держит его endpoint, core через Service `harbor-registry` получает `dial tcp 10.96.35.155:5000: connect: connection refused` (`http: proxy error` в логе core) и отвечает клиенту 502; docker-клиент часть таких случаев повторяет сам. В chart есть `terminationGracePeriodSeconds: 120`, но нет `preStop` и нет значений для него.
  - **Исправление:** `hack/helm-postrender.py` — post-renderer Helm (подключён в `hack/install-harbor-ha.sh`, `--post-renderer`), добавляет `lifecycle.preStop: sleep 15` всем контейнерам core, registry (registry, registryctl) и portal: старый под ещё обслуживает запросы, пока маршрутизация убирает его. Требует python3 + PyYAML на хосте. В образах есть `sh` и `sleep` (проверено). Применено `make harbor-ha` (helm revision 5), распределение по нодам после rollout 1+1.
  - **Замер после исправления, 4 прогона:** 0 ошибок — 0/746 запросов манифеста, 0/340 блоба, 0/480 `docker pull`; 4 прогона из 4 чистые; самый медленный запрос 0,5 с (5-секундные паузы исчезли). Суммарно за 8 прогонов: до — 2 ошибки в 2 из 4 прогонов, после — ни одной. **Оговорка:** выборка небольшая (ошибка до исправления возникала примерно в 1 из 350–750 запросов), «0 ошибок» подтверждает улучшение, но не доказывает нулевую вероятность; UI (portal) нагрузкой не покрывался, хотя preStop для него тоже добавлен; jobservice не менялся (клиентский путь pull его не использует).
  - **Итог для критерия H4.3:** после исправления rolling update core и registry проходит без ошибок у клиента. До исправления критерий выполнялся не полностью (редкие 502) — это найденный и устранённый дефект стенда, а не свойство Harbor.
- [ ] **H4.4** Потеря worker-ноды (если D1 = multi-node): сервис продолжает отвечать.
- [ ] **H4.5** demo-app: rollout после push нового тега, поды подтягивают образ; по `Hello, Kube! (from <pod>)` видно распределение по репликам.
- [ ] **H4.6** Proxy-cache: проект-прокси к внешнему registry (например Docker Hub) — pull через Harbor с любой реплики, повторный pull отдаётся из кэша; локальные проекты продолжают работать рядом.
- [ ] **H4.7** Отказ по ролям схемы: по очереди остановить `app` (1 из 2), Harbor LB (1 из 2), `pg` (primary — Patroni переключает через Consul), `redis` (master — Sentinel), Consul (по D1); сервис отвечает, push/pull проходят.

### Phase 5 — Документация

- [ ] **H5.1** `README.md`: HA-раздел, актуальные примеры вывода.
- [ ] **H5.2** `AGENTS.md` / `CLAUDE.md` под фактическое состояние.
- [ ] **H5.3** Acceptance с нуля: `cluster-delete` → `cluster` → зависимости → `install` (HA) → `deploy-app` → проверки Phase 4.
- [ ] **H5.4** Перевести `docs/verification-runbook.md` в Ansible-плейбук (`ansible/verify.yml`, теги V1..V10, `kubernetes.core`, сводный отчёт и ненулевой код возврата при отказе); план соответствия модулям — в конце ранбука.

## Риски и заметки

- 14 нод-контейнеров на одной машине: без Harbor измерено ≈ 3,9 ГиБ (см. «Аудит перед Phase 3»), со всем стендом оценка ≈ 12–13 ГБ при ≈ 27–30 ГБ доступных; при нехватке ресурсов сокращать по согласованию (например, число Consul-серверов), не молча.
- Один хост Docker: «HA» здесь учебная — общий диск, общее ядро, реальной изоляции отказов нет.
- Самоподписанный CA Harbor пересоздаётся при каждой установке → доверие на ноде надо обновлять после каждого `install` (`make deploy-app` это уже делает).
- Ресурсы: PostgreSQL + Redis + MinIO + N реплик Harbor на локальной машине — оценить RAM до старта.
- Уроки базовой линии, которые легко повторить: непинованные зависимости ломают сборку со временем (случай Flask/Werkzeug); MetalLB L2 не держит анонс сервиса без Ready-эндпоинтов — при «зависшем» LB IP сначала смотреть `kubectl get endpoints`.
