# ansible-galaxy collection install -r ansible/requirements.yml
# Python side: pip install --user kubernetes   (the kubernetes.core modules need it)
collections:
  - name: kubernetes.core
    version: ">=3.2.0"
